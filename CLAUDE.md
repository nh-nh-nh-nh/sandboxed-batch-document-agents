# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty — it contains only a Python-oriented `.gitignore` and no source code, dependency manifest, README, or project structure yet. The `.gitignore` suggests the project will be Python-based, but no framework, package manager, or layout has been chosen.

There are no build, lint, or test commands to document, and no architecture to describe, because none exists yet.

When code is added to this repository, update this file with:
- The actual build/lint/test commands (and how to run a single test)
- The high-level architecture once there are multiple files/modules whose relationships aren't obvious from directory structure alone
